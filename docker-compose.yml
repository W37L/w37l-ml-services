version: "3.8"

services:

  api:
    build: .
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - ./src:/mlservice
      - ./tests:/tests
    command:
      - uvicorn
      - app:app
      - --host 0.0.0.0
      - --port 8000
    ports:
      - "5005:8000"